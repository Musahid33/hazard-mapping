# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.vscode/
.idea/

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (keep .env.example, ignore real .env)
.env
.env.local
.env.production

# Surge.sh local config (not needed for GitHub)
CNAME
.surgeignore

# Shared-data store (file fallback, created by server.js at runtime — do not commit live data)
# Supabase is primary for live, file is only for local dev fallback
data/
*.log

# Build outputs
dist/
build/
.next/
out/

# Supabase local
.supabase/

# Vercel CLI local link
.vercel/
